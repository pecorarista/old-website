extends ../_layout

block content
  h2 Posts
  ul
    for post in posts
      li
        a(href=`/ja/posts/${post.id}.html`)= post.titles.ja
        | &nbsp;–&nbsp;
        span.strong= post.created
        | &nbsp;(modified:&nbsp;
        span= post.modified
        | )
